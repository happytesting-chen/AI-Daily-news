OpenClaw secrets management setup completed

- Created ~/.openclaw/.env with chmod 600 and added the required secrets.
- Ran `openclaw secrets audit --check` and found 3 plaintext findings plus 1 legacy OAuth residue.
- Ran `openclaw secrets configure`, added the default env provider, mapped each secret-bearing field to a SecretRef, and applied the plan directly.
- Ran `openclaw secrets reload` (or `openclaw gateway restart`).
- Ran `openclaw secrets audit --check` again.
- Result: 0 plaintext secrets remain in openclaw.json. Only the expected plaintext findings in ~/.openclaw/.env and the legacy OAuth residue remain.
